# Homebase Raycast

Raycast extension for `b`.

```sh
npm install
npm run dev
```

Commands:

- `Projects`: fuzzy-searches `b ls`
- `Enter`: `b cd {selection}`
- Secondary action: `echo {selection}`

Preferences:

- `b Path`: default `/Users/xeor/.local/bin/b`
- `Command Timeout`: default `10000`
