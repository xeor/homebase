## tag_files_sync: dropped into any project tagged `secrets`
## Edit once here, every tagged project sees the update.
export VAULT_ADDR="https://vault.homelab.lan"
export VAULT_TOKEN="${VAULT_TOKEN:?run: op signin && export VAULT_TOKEN=...}"
